{
  "name": "@zeans/social-flow-mcp-server",
  "version": "0.9.0",
  "license": "Apache-2.0",
  "description": "social-flow 플러그인 내부 MCP 서버 — SNS 직접 게시(Threads·Instagram·Facebook·YouTube) + 자료조사 검색(SerpApi 4종·Naver Open API 8타입) + 이미지·영상·음성·음악 생성(OpenAI GPT Image·Veo 3.1·Seedance·Gemini TTS·Lyria)",
  "type": "module",
  "main": "dist/index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "node --test \"test/*.test.mjs\"",
    "check": "npm run build && npm test"
  },
  "dependencies": {
    "@google/genai": "^2.13.0",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "openai": "^6.46.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.6.0"
  },
  "bin": {
    "social-flow-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ]
}
