# 렌더 성능 최적화 — 적대적 평가 루브릭 (고정, 2026-09-04)

목표 문장: "이 플러그인의 성능 최적화가 가능한 부분을 분석하고 개선한다. 적대적 평가 95점 이상, P0 0."
통과선은 95점 · P0 0 이다(loop-verifier 기본 90 이 아니라 사용자 요구 95).

점수는 0에서 시작해 근거(file:line, 명령+종료 코드, 기록된 수치)가 있는 항목만 더한다.

| # | 기준 | 배점 | 채점 규칙 | P0 |
|---|------|-----|-----------|----|
| 1 | 실측 속도 향상 | 25 | 같은 슬라이드·같은 옵션으로 개선 전/후를 잰 표가 `bench.tsv` 에 있고 실사(footage) 슬라이드 2장 이상(그중 하나는 ep209 s5 또는 s4·s14 처럼 1fps 아래였던 장) + 실사 아닌 모션 슬라이드 1장이 들어 있다. 실사 슬라이드 시간이 이전의 1/2 이하 → 15점, 1/4 이하 → 25점. 모션 슬라이드가 느려졌으면 0점. | ○ |
| 2 | 결과물 동등성 | 20 | 개선 후 렌더가 이전 렌더와 그룹 수·프레임 수·durations_ms·manifest 행이 같고 모든 프레임이 픽셀 동일하거나 PSNR 50dB 이상(비교 스크립트 출력이 기록돼 있어야 한다). 실사 슬라이드와 모션 슬라이드 둘 다 비교. footage-lane.md 의 이음매 성질(그룹 k 의 f0000 == 그룹 k−1 의 sheet end)이 새 렌더에서도 성립. 둘 중 하나라도 빠지면 0점. | ○ |
| 3 | 계약 실패 보존 | 15 | 렌더러의 문서화된 실패 경로가 그대로 exit 1 인 것을 실제로 돌려 확인: seek API 없는 페이지, 그룹 밖 애니메이션, 없는 소재. `check-slide.js --selftest` 종료 코드 0. 셋 중 하나라도 확인 안 되면 0점. | ○ |
| 4 | 결정성·병렬 | 15 | 새 모드로 두 번 연속 렌더한 결과가 동일(또는 헤더 주석이 말하는 서브픽셀 흔들림 범위) → 8점. 실사 슬라이드에서 `--jobs` 2 이상이 CDP 타임아웃 없이 끝나고 결과가 jobs 1 과 같음 → 7점(안 되면 그 사실과 수치를 문서에 적고 0점). | |
| 5 | 린트 | 10 | `skill-lint.js`·`pipeline-lint.js`·`format-lint.js` 종료 코드 0, `node --check` 렌더러 0. 하나라도 실패면 0점. | |
| 6 | 문서·문체 | 10 | 렌더러 헤더 주석의 fps·대기 방식 설명이 실측 수치로 갱신됐고 스킬 문서는 명령이나 jobs 규칙이 바뀐 자리만 고쳤다(한 훈크). CLAUDE.md 의 D8 스캔이 건드린 파일에서 깨끗하고 연결어미 뒤 쉼표 없음. | |
| 7 | 조사 보고 | 5 | 재 봤지만 손대지 않은 지렛대(인트라 전용 트랜스코드, MCP 서버 기동, 검사기, build-reel 프로파일, SKILL.md 크기, 에이전트 설명 길이)가 수치와 이유와 함께 `index.html` 에 적혀 있다. | |

합계 100. P0 표시 항목이 기준 미달이면 점수와 무관하게 FAIL.

## 2026-09-04 구현 중 확인한 사실 — 기준 2 의 판정 근거

기준 2 는 "이전 렌더와 모든 프레임이 같다"인데 이전 렌더러 자체가 같은 조건에서 두 번 돌리면 그룹 첫 컷 직후 프레임(f0001)이 서로 달랐다(원본 A 대 A2 는 133/133 같았지만 원본 전체 렌더 OLD1 은 r1 f0001 이 원본 클립 0번 프레임에 멈춰 있었고 다른 원본 실행은 1번이었다). 기준 문구는 그대로 두되 그 프레임의 판정은 이렇게 한다. 새 렌더 세 번이 서로 같고 그 프레임이 ffmpeg 로 푼 원본 클립의 프레임과 대조해 맞으면 동등하다고 본다. 이전 렌더와 다른 프레임은 이전 렌더가 틀린 프레임이어야 하고 그 근거(원본 대조 출력)가 문서에 있어야 한다.

## 판정 기록

| 회차 | 커밋 | 점수 | P0 | 판정 | 감점 |
|---|---|---|---|---|---|
| 1 | 1b7f41d | 93 | 0 | FAIL | 기준 4 의 jobs 1 대 jobs 3 동일성 7점 — 그룹 첫 프레임이 탭에 따라 2장 달랐다(457/459) |
| 2 | 67375f9 | 100 | 0 | PASS | 없음 — f0000 을 앞 그룹의 끝 프레임으로 써서 459/459 |

검증자는 `astra-methodology:loop-verifier`, 통과선 95 · P0 0. 두 회차 모두 픽스처·린트·비교 스크립트를 검증자가 직접 다시 돌렸다.
